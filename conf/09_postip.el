(bundle pos-tip)
(require 'pos-tip)

(setq pos-tip-foreground-color "white")
(setq pos-tip-background-color "black")

;; (bundle tooltip :url "https://github.com/emacs-mirror/emacs/raw/master/lisp/tooltip.el")
;; (require 'tooltip)

